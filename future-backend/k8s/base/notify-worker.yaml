---
# Future Backend - Matching Engine Notify Worker
# Consumes from Kafka matching_engine_output and broadcasts to WebSocket clients
apiVersion: apps/v1
kind: Deployment
metadata:
  name: future-notify-worker
  namespace: future-backend
  labels:
    app: future-notify-worker
    component: worker
    app.kubernetes.io/name: future-backend
    app.kubernetes.io/component: notify-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: future-notify-worker
  template:
    metadata:
      labels:
        app: future-notify-worker
        component: worker
        app.kubernetes.io/name: future-backend
        app.kubernetes.io/component: notify-worker
    spec:
      containers:
        - name: notify-worker
          image: exchange/future-backend:main
          imagePullPolicy: Always
          command: ["node"]
          args: ["dist/console.js", "matching-engine:notify"]
          envFrom:
            - secretRef:
                name: future-backend-secrets
          volumeMounts:
            - name: config-volume
              mountPath: /app/config/local.yml
              subPath: local.yml
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - "pgrep -f 'matching-engine:notify' > /dev/null"
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
      volumes:
        - name: config-volume
          configMap:
            name: future-backend-config
      nodeSelector:
        kubernetes.io/os: linux
      restartPolicy: Always
