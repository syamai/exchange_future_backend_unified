//FUNDING_RATE.NEXT_TIME- 8h - 3600000*8
export enum FUNDING_RATE {
  DEFAULT = "0.01",
  NEXT_TIME = 28800000,
}
// "6" just example value that was used in funding rate calculation file of BA
export enum IMN_COINM {
  VALUE = 6,
}
