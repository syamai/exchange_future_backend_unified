apiVersion: v1
kind: Secret
metadata:
  name: matching-engine-secrets
  namespace: matching-engine
  labels:
    app.kubernetes.io/name: matching-engine
type: Opaque
stringData:
  # Kafka connection (override in overlays)
  KAFKA_BOOTSTRAP_SERVERS: "kafka:9092"

  # Optional: Kafka authentication
  # KAFKA_SASL_USERNAME: ""
  # KAFKA_SASL_PASSWORD: ""
