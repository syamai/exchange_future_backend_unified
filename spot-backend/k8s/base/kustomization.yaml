apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: spot-backend-base

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/name: spot-backend
  app.kubernetes.io/part-of: exchange
